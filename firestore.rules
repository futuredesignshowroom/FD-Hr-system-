rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Simple authentication check
    function isAuth() {
      return request.auth != null;
    }

    // Users collection - Simplified to avoid circular dependencies
    match /users/{userId} {
      // Users can read their own document
      allow read: if isAuth() && request.auth.uid == userId;
      // Users can create their own profile
      allow create: if isAuth() && request.resource.data.id == request.auth.uid;
      // Users can update their own profile
      allow update: if isAuth() && request.auth.uid == userId;
      // No one can delete
      allow delete: if false;
    }

    // Attendance collection - Authenticated users can read all
    match /attendance/{document=**} {
      allow read: if isAuth();
      allow write: if false;
    }

    // Leaves collection - Authenticated users can read all
    match /leaves/{document=**} {
      allow read: if isAuth();
      allow write: if false;
    }

    // Salary collection - Authenticated users can read all
    match /salary/{document=**} {
      allow read: if isAuth();
      allow write: if false;
    }

    // Leave Config collection
    match /leaveConfig/{document=**} {
      allow read: if isAuth();
      allow write: if false;
    }

    // Salary Config collection
    match /salaryConfig/{document=**} {
      allow read: if isAuth();
      allow write: if false;
    }

    // Messages collection
    match /messages/{document=**} {
      // Users can read their own messages
      allow read: if isAuth() && (
        resource.data.senderId == request.auth.uid ||
        resource.data.recipientId == request.auth.uid
      );
      // Users can send messages
      allow create: if isAuth() && request.resource.data.senderId == request.auth.uid;
      allow update, delete: if false;
    }

    // Deny all other access
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
